# Provenance

The raw values in this bundle are a tiny manually selected transcription of 12 records from the classic Iris dataset as publicly distributed by the UCI Machine Learning Repository.

Bundle purpose:

- use a tiny real, non-sensitive measurement table
- encode two exact observer panels:
  - sepal panel
  - petal panel
- preserve the distinction between exact extracted rows and derived covariance objects
