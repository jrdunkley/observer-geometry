# Replication Protocol Encoding

This example encodes protocol descriptions as finite observer hypotheses.

- protocol facts are exact evidence
- observer matrices are encoded inference
- the selected observer family is recorded as load-bearing
- `nomodescent` then checks the relation between the resulting observers

The point is to show how different protocols can be encoded honestly before any
compatibility or mismatch conclusion is computed.
