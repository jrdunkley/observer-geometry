# Result

- Exact table slice rejects a naive shared observer.
- Recommended richer-blue observer reduces the linear contradiction by about `35.8x`.
- Even that richer encoding still lands in `incompatible_by_linear_inconsistency`.
- Use this as a protocol-mismatch probe, not a final calibration verdict.
