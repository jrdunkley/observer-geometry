# Result

- `power_only` is a coarsening of both richer observers.
- `power_escape` and `power_geometry` are non-nested.
- The richer pair stays `underdetermined_affine_family`.
- Keep the observer-structure result. Treat the full three-surface inconsistency as a rounded-table overconstraint, not the headline.
